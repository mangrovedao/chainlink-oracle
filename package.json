{
  "name": "chainlink-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph codegen && graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ @mangrovedao/chainlink-subgraph",
    "create-local": "graph create --node http://localhost:8020/ @mangrovedao/chainlink-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ @mangrovedao/chainlink-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 @mangrovedao/chainlink-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.50.1",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
